[project]
name = "twitter-gig-hunter"
version = "1.0.0"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
timeout = 30
markers = [
    "integration: marks tests requiring ZeroClaw or network (deselect with '-m not integration')",
]
